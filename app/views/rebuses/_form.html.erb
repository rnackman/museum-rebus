<%= form_for(@rebus, remote: true) do |f| %>
  <% if @rebus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rebus.errors.count, "error") %> prohibited this rebus from being saved:</h2>

      <ul>
      <% @rebus.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.label :input_text %>
    <%= f.text_area :input_text, id: 'input_text', placeholder: 'Enter text here.' %>
    <%= f.submit %>
  </div>
<% end %>
